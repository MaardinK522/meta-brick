import 'package:flutter/material.dart';

class UserProfilePageRoute extends StatelessWidget {
  const UserProfilePageRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("User Profile Page Route"),
      ),
    );
  }
}
