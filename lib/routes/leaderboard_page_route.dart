import 'package:flutter/material.dart';

class LeaderboardPageRoute extends StatelessWidget {
  const LeaderboardPageRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Leader"
            "board page route"),
      ),
    );
  }
}
